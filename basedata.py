"""
                current_table=current_base['help'+input_list[2]]
                rows=current_table.rows
                for row in rows:
                   row_val = [col.value for col in row]
                   print(row_val)
                   #print(cell.value)"""
                    #columns_list = re.findall('\((.*)\)',name)[0].split(',')
                #result=re.search('create table (.*) \((.*)\);$',name)
                   # dbpath=path+'/base/'+input_list[1]+'/'